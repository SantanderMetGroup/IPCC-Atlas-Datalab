{
  "postCreateCommand": {
    "Interactive Atlas DataLab environment": ["bash", "-c", "conda install -c conda-forge -y -n base mamba && mamba env update -n base -f .binder/environment.yml"]
  },
  "features": {}
}
